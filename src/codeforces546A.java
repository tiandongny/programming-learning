/*
import java.util.Scanner;

public class codeforces546A {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int bananaPrice = sc.nextInt();
        int initialBalance = sc.nextInt();
        int numBananas = sc.nextInt();
        int totalPrice = bananaPrice * (numBananas * (numBananas + 1) / 2);
        if (initialBalance >= totalPrice){
            System.out.println(0);
        } else {
            int borrowedMoney = totalPrice-initialBalance;
            System.out.println(borrowedMoney);
        }
    }
}
*/